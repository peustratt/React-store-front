import styled from 'styled-components';

const Div = styled.div`
    display: flex;
    flex-direction: column;
    align-items: flex-end;
    width: fit-content;
    gap: 40px;
    background: #fff;
    position: absolute;
    right: 100px;
    max-height: 80vh;
    overflow-y: auto;
    z-index: 2;
    padding: 2rem 1rem;
`

export default Div;