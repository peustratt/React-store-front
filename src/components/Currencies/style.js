import styled from 'styled-components';

const Ul = styled.ul`
    background: red;
    text-align: right;
`

const Modal = styled.div`
    position: fixed;
    inset: 0;
    z-index: 2;
`

export {Ul, Modal};